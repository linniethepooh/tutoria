<h2>Edit company - <%= @company.name %></h2>

<%= simple_form_for @company do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :city %>
  <%= f.input :zipcode %>
  <%= f.input :country, as: :text %>

<%= f.submit "Edit company", class: "btn btn-primary" %>
<% end %>
