function refreshForm(innerHTML) {
  const newReviewForm = document.querySelector('.steps-area');
  newReviewForm.innerHTML = innerHTML;
}

<% if @step.errors.any? %>
  alert("An error occured while saving the step")
<% else %>
    refreshForm('<%= j render "tutorials/steps-partial", steps: @steps %>');
<% end %>






